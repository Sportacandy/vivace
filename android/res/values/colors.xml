<?xml version='1.0' encoding='utf-8'?>
<resources>
    <!-- Background layer for the adaptive launcher icon
         (mipmap-anydpi-v26/ic_launcher.xml). The foreground artwork's
         own background is transparent, so a plain solid fill is needed
         behind it once the OS applies its own icon mask shape. -->
    <color name="ic_launcher_background">#FFFFFF</color>
</resources>
